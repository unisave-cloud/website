{
  "name": "unisave-website",
  "version": "latest",
  "source": [
    "src/index.html"
  ],
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "parcel",
    "build": "parcel build --public-url ./"
  },
  "devDependencies": {
    "parcel": "^2.8.3"
  }
}
